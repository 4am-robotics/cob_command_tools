rosbag_play_frequency: 1 # hz
publish_rate: 1 # hz

# frames
path_frame_id: map

# tf map to base_link ground truth
# run in terminal: rosrun tf2_tools echo.py map base_link -t 0
path_target_xyz_quat: [-11.023, 5.422, 0.000, 0.000, 0.000, 0.694, 0.719]

# interactive marker
interactive_marker_scale_xyz: [0.4, 0.4, 0.4]
interactive_marker_color_rgba: [0, 1, 0, 1]
interactive_marker_orientation_quat: [0, 0.7071, 0, 0.7071]
interactive_marker_server: interactive_marker
interactive_marker_name: path_marker
interactive_marker_description: "Drag to create path"

# parsed via launch:
# - map_bag_file
